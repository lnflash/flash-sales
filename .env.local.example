# GraphQL API Configuration
# Optional - Used for username verification during login
# If not provided, development mode will bypass GraphQL verification
# In production, this should point to the Flash API GraphQL endpoint
# NEXT_PUBLIC_GRAPHQL_URI=https://api.getflash.com/graphql


# Intake API URL (removed - now using Supabase)
# INTAKE_API_URL=<removed>